\chapter{Release~2: Transactions, Intelligence \& Delivery}
\label{chap:release2}

\section*{Introduction}

This chapter covers Release~2, \textit{Transactions, Intelligence \&
Delivery}, which turns the populated marketplace into a transacting,
self-moderating and deployable product. After a release overview,
use-case diagram, domain model and backlog, it presents the release's
two sprints: \textbf{Sprint~3 (Orders, Payment \& Administration)}
delivers the cart, multi-store checkout, Stripe payment and
administration tooling; \textbf{Sprint~4 (AI Verification \& Premium)}
adds the AI verification pipeline, the premium tier, and the testing and
deployment that bring the platform to production.

\section{Release 2 Overview}

Release~2 completes the marketplace over two sprints. Sprint~3 makes the
catalogue transactable: customers fill a cart, check out across multiple
stores in a single order, and pay through Stripe, while administrators
moderate stores and products, curate featured content and manage
categories. Sprint~4 adds the intelligence and delivery layers: automated
AI verification of new stores and products, the AI database-chat
assistant, live cross-store product suggestions on the product page, the
premium subscription tier, a light/dark theming option, and the test suite
and deployment pipeline. As before, the architecture and technology stack of
Section~\ref{chap:requirements} are not repeated; this release
instantiates them for the transactional, intelligence and delivery
features.

\subsection{Release 2 Domain Model}

Figure~\ref{fig:class-release2} shows the slice of the domain model
exercised by Release~2: the \texttt{Order} and its \texttt{OrderItem}s,
the payment fields stamped on the order, the verification flags on
\texttt{Store} and \texttt{Product}, the \texttt{StoreStatusLog} audit
trail and the featured-content entities.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/class-release2.png}
  \caption{Release~2 domain model (subset of the platform class diagram).}
  \label{fig:class-release2}
\end{figure}

\section{Release~2 Backlog}

The release backlog draws the remaining user stories from the product
backlog of Section~\ref{chap:requirements}, including the payment
story US36, the account-management stories US38, US45 and US39 and the
theming story US40, and allocates them to the two sprints, as summarised
per sprint in Tables~\ref{tab:backlog-r2-s3} and~\ref{tab:backlog-r2-s4}.
As in Release~1, each story carries an estimated duration in working days
that adds up to its sprint envelope.

\begin{table}[H]
  \centering
  \caption{Release~2 backlog --- Sprint~3 (orders, payment and administration).}
  \label{tab:backlog-r2-s3}
  \footnotesize
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{|l|X|>{\centering\arraybackslash}p{1.6cm}|}
    \hline
    \rowcolor{tableHeader}
    \textcolor{white}{\textbf{Feature}} & \textcolor{white}{\textbf{User story}} &
    \textcolor{white}{\textbf{Est. (days)}} \\
    \hline
    \multirow{5}{*}{\makecell[l]{Order System\\\& Payment}} & US14 shopping cart & 3 \\ \cline{2-3}
      & US15 multi-store checkout & 5 \\ \cline{2-3}
      & US16 order-confirmation email & 1 \\ \cline{2-3}
      & US17 seller order view & 3 \\ \cline{2-3}
      & US36 secure card payment & 6 \\ \hline
    \multirow{3}{*}{\makecell[l]{Account\\Management}} & US38 profile & 2 \\ \cline{2-3}
      & US45 change password & 2 \\ \cline{2-3}
      & US39 purchase history & 2 \\ \hline
    \multirow{3}{*}{Administration} & US19 store suspension/deletion & 2 \\ \cline{2-3}
      & US20 featured-content curation & 2 \\ \cline{2-3}
      & US21 category management & 2 \\ \hline
    \multicolumn{2}{|r|}{\textit{Sprint~3 subtotal}} & \textit{30 (\textasciitilde1.5 mo.)} \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Release~2 backlog --- Sprint~4 (AI verification, premium tier, theming and deployment).}
  \label{tab:backlog-r2-s4}
  \footnotesize
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{|l|X|>{\centering\arraybackslash}p{1.6cm}|}
    \hline
    \rowcolor{tableHeader}
    \textcolor{white}{\textbf{Feature}} & \textcolor{white}{\textbf{User story}} &
    \textcolor{white}{\textbf{Est. (days)}} \\
    \hline
    \multirow{4}{*}{AI Verification} & US22 AI store/product verification & 3 \\ \cline{2-3}
      & US23 AI verdict override & 1 \\ \cline{2-3}
      & US24 verified badge & 1 \\ \cline{2-3}
      & US35 AI storefront redesign & 3 \\ \hline
    Product Suggestions & US41 cross-store product suggestions & 3 \\ \hline
    \multirow{3}{*}{Premium Tier} & US25 premium subscription & 3 \\ \cline{2-3}
      & US26 premium customisation controls & 2 \\ \cline{2-3}
      & US27 premium preservation on expiry & 2 \\ \hline
    Appearance \& Theming & US40 light/dark theming & 2 \\ \hline
    \multirow{6}{*}{\makecell[l]{CI/CD \&\\Deployment}} & US28 service containerisation & 2 \\ \cline{2-3}
      & US29 CI image builds & 2 \\ \cline{2-3}
      & US30 Terraform provisioning & 2 \\ \cline{2-3}
      & US31 SSH deployment & 2 \\ \cline{2-3}
      & US32 CI-managed secrets & 1 \\ \cline{2-3}
      & US33 DNS automation & 1 \\ \hline
    \multicolumn{2}{|r|}{\textit{Sprint~4 subtotal}} & \textit{30 (\textasciitilde1.5 mo.)} \\ \hline
    \multicolumn{2}{|r|}{\textbf{Release~2 total}} & \textbf{\textasciitilde3 months} \\ \hline
  \end{tabularx}
\end{table}

\section{Sprint 3, Orders, Payment \& Administration}

\subsection{Sprint 3 Objective}

Sprint~3 makes the marketplace transactable and governable. By the end of
the sprint a customer can build a cart, check out across several stores
in a single order, and pay securely through Stripe, receiving a
confirmation email; a seller can see the orders that include their
products; every signed-in user can manage their own account, viewing
and editing their profile, changing their password and reviewing their
purchase history, and an administrator can moderate stores and
products, suspend or delete a store, curate featured content and manage
the category tree.
The payment flow is the centrepiece: amounts are computed server-side,
charged through a single Stripe~\cite{stripe} PaymentIntent, and confirmed
asynchronously by a signature-verified webhook.

\subsection{Sprint 3 Use-Case Diagram}

Figure~\ref{fig:uc-sprint3} details the sprint's use cases. Checkout
\emph{includes} the secure payment, which in turn drives the Stripe
gateway and the confirmation email; suspending a store is an
\emph{extension} of store moderation.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/uc-sprint3.png}
  \caption{Sprint~3 use-case diagram (orders, payment and administration).}
  \label{fig:uc-sprint3}
\end{figure}

\subsection{Analysis and Design}

The checkout-and-payment control flow is summarised in
Table~\ref{tab:act-checkout-desc} and then modelled as an activity
diagram. The Stripe payment mechanism is detailed in its own section.

\subsubsection{Activity Diagram}

\begin{table}[H]
  \centering
  \caption{Textual description of the checkout-and-payment activity.}
  \label{tab:act-checkout-desc}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3.2cm}|X|}
    \hline
    Title & Checkout and secure payment \\ \hline
    Objective & Convert a multi-store cart into a single order and collect payment securely through Stripe. \\ \hline
    Primary actor & Customer (authenticated, verified). \\ \hline
    Secondary actor & Payment Gateway (Stripe). \\ \hline
    Pre-condition & The cart is non-empty and the customer is signed in with a verified email. \\ \hline
    Nominal scenario & 1.~The customer opens checkout and confirms the items and shipping details. \newline 2.~The server prices each line server-side, reserves stock, and creates one \textsc{pending} order spanning all stores. \newline 3.~The server opens a Stripe PaymentIntent for the server-computed total and stamps its id on the order. \newline 4.~The customer confirms payment with Stripe. \newline 5.~On a signature-verified success webhook the order becomes \textsc{paid} and \textsc{confirmed} and a confirmation email is sent. \\ \hline
    Alternative scenario & A1.~Insufficient stock on a line: the line is flagged and checkout halts. \newline E1.~Payment fails or is canceled: the order is marked failed and the reserved stock is released. \newline E2.~Lost webhook: a periodic reconciliation sweep settles or restocks the order. \\ \hline
    Post-condition & A \textsc{paid}, \textsc{confirmed} order exists, or the reserved stock is released on failure. \\ \hline
  \end{tabularx}
\end{table}

Figure~\ref{fig:act-checkout-payment} models the checkout-and-payment
control flow. After stock is reserved and the pending order priced
server-side, a single Stripe PaymentIntent is opened; the signed webhook
then drives the order either to \textsc{paid}/\textsc{confirmed} on
success or back to released stock on failure.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.82\textheight,keepaspectratio]{img/act-checkout-payment.png}
  \caption{Activity diagram, multi-store checkout and Stripe payment.}
  \label{fig:act-checkout-payment}
\end{figure}

\subsection{Stripe Payment Design}

Payment is the most security-sensitive flow of the platform, and its
design follows a single principle: \textbf{the client never dictates what
it is charged}. When the customer checks out, the backend reserves stock,
creates the pending order(s) for the cart, and computes the grand total
from the server-side prices. Stripe accounts cannot settle in Tunisian
dinar, so the dinar total is converted to United States dollar minor
units through a configurable rate (\texttt{TND\_TO\_USD\_RATE}) while the
interface continues to display dinar. A single PaymentIntent is then
opened for the whole cart, even when it spans several stores, with
an \textbf{idempotency key} derived from the order identifiers, so that a
retried request never creates a duplicate charge. The PaymentIntent
identifier is stamped on the order(s) for later reconciliation.
Listing~\ref{lst:paymentintent} shows the core of this server-side
charge.

\begin{lstlisting}[caption={Server-side PaymentIntent creation (amount derived on the server).},label={lst:paymentintent}]
const intent = await stripe.paymentIntents.create(
  {
    // amount is computed server-side and converted TND -> USD minor units
    amount: tndToStripeUsdMinor(grandTotal),
    currency: 'usd',
    metadata: {
      orderIds: orderIds.join(','),
      displayCurrency: 'TND',
      displayAmount: grandTotal.toFixed(3),
    },
  },
  { idempotencyKey: `checkout-${orderIds.join('-')}` },
);
await orderService.setPaymentIntent(orderIds, intent.id);
\end{lstlisting}

Confirmation is asynchronous. Stripe calls an \textbf{unauthenticated}
\texttt{POST /payments/webhook} endpoint, it is deliberately public,
because the caller is Stripe rather than a logged-in user. Trust is
established not by a session but by \textbf{verifying the Stripe
signature against the raw request body}; an unverifiable payload is
rejected. On a \texttt{payment\_intent.succeeded} event the order(s) are
marked \textsc{paid} and \textsc{confirmed} and the confirmation email is
sent; on a failed or canceled event the reserved stock is released. The
handler is idempotent, so a duplicated webhook is harmless. Finally, a
periodic cleanup sweep reconciles checkouts left unpaid past a staleness
window: it retrieves each PaymentIntent from Stripe and either settles a
paid-but-unacknowledged order or releases the stock of an abandoned one,
so a lost webhook can never strand stock or a payment.

\subsection{Realisation}

The sprint delivers the transaction and administration interfaces shown
below. Each frame is a placeholder for the corresponding screenshot of
the running application.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Checkout page, multi-store cart summary, shipping form and Stripe payment element}
  \caption{Checkout page with Stripe payment.}
  \label{fig:real-checkout}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Account page, profile view/edit (name, phone, avatar), change-password form and the paginated purchase history}
  \caption{User account and profile page.}
  \label{fig:real-profile}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Administrator dashboard, marketplace metrics and navigation}
  \caption{Administrator dashboard.}
  \label{fig:real-admindash}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Moderation panel, stores and products with suspend / delete and verdict-override actions}
  \caption{Store and product moderation panel.}
  \label{fig:real-moderation}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Category management, category and sub-category tree}
  \caption{Category management.}
  \label{fig:real-categories}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Featured-content curation, selecting featured stores and products for the landing page}
  \caption{Featured-content curation.}
  \label{fig:real-featured}
\end{figure}

\section{Sprint 4, AI Verification \& Premium Tier}

\subsection{Sprint 4 Objective}

Sprint~4 adds the intelligence and monetisation layers. Every new store
and product is screened automatically by an AI verification pipeline
before it becomes visible; suspicious content is flagged for the
administrator, who remains the final authority. Administrators gain a
natural-language database chat assistant for diagnostics; buyers see live
cross-store product suggestions, a cheaper same-item offer and relevant
alternatives, on every product page; and sellers
, and only sellers, can take out a \emph{recurring monthly} premium
subscription through Stripe Checkout, managed afterwards through the Stripe
Customer Portal, that unlocks the full customisation set and multiple
stores; this is a new payment mechanism, distinct from the one-time charge
of Sprint~3. The AI
capabilities are delivered through four independent n8n workflows
invoked by the backend over HTTP. Finally, the sprint rounds out the user
experience with an appearance option: a light and a dimmed dark theme,
chosen explicitly or following the operating-system preference and
applied consistently across the application chrome through CSS design
tokens.

\subsection{Sprint 4 Use-Case Diagram}

Figure~\ref{fig:uc-sprint4} details the sprint's use cases. Automatic
verification \emph{includes} returning a verdict with a rationale and may
\emph{extend} into flagging content for the administrator to override;
subscribing to premium \emph{unlocks} the advanced customisation and
multiple stores through the Stripe gateway, and the subscription is
afterwards managed through that same gateway.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/uc-sprint4.png}
  \caption{Sprint~4 use-case diagram (AI verification and premium tier).}
  \label{fig:uc-sprint4}
\end{figure}

\subsection{Analysis and Design}

The AI-verification and content-moderation control flows are summarised
in Tables~\ref{tab:act-aiverify-desc} and~\ref{tab:act-moderation-desc}
and then each modelled as an activity diagram.

\subsubsection{Activity Diagrams}

\begin{table}[H]
  \centering
  \caption{Textual description of the AI-verification activity.}
  \label{tab:act-aiverify-desc}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3.2cm}|X|}
    \hline
    Title & AI verification pipeline \\ \hline
    Objective & Automatically screen every new store and product before it becomes visible. \\ \hline
    Primary actor & AI Service (n8n + vision LLM), triggered by store or product creation. \\ \hline
    Secondary actor & Administrator (for flagged entities). \\ \hline
    Pre-condition & A new entity exists with \texttt{aiReviewed = false} and \texttt{verificationStatus = pending}, with its images uploaded. \\ \hline
    Nominal scenario & 1.~After image upload, the backend fires the \texttt{ai-verify} webhook with the listing text and image list. \newline 2.~n8n downloads each image and submits it with the text to the vision model. \newline 3.~The per-image verdicts are aggregated, flagging the entity if any image is flagged. \newline 4.~The decision is posted to the \texttt{/ai-review} callback. \newline 5.~The backend publishes clean entities and queues flagged ones for moderation. \\ \hline
    Alternative scenario & E1.~The webhook or model fails: the entity stays \textsc{pending} and is surfaced in the administrator's queue for manual review. \\ \hline
    Post-condition & \texttt{aiReviewed} is \texttt{true}; the entity is auto-published or flagged for administrator review, with a recorded verdict and rationale. \\ \hline
  \end{tabularx}
\end{table}

Figure~\ref{fig:act-ai-verification} models the AI verification pipeline,
including the per-image screening loop inside the n8n workflow and the
branch that either publishes a clean entity or routes a flagged one to
moderation.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.82\textheight,keepaspectratio]{img/act-ai-verification.png}
  \caption{Activity diagram, AI verification pipeline.}
  \label{fig:act-ai-verification}
\end{figure}

\begin{table}[H]
  \centering
  \caption{Textual description of the content-moderation activity.}
  \label{tab:act-moderation-desc}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3.2cm}|X|}
    \hline
    Title & Content moderation \\ \hline
    Objective & Let an administrator arbitrate a flagged store or product, overriding the AI verdict or suspending offending content. \\ \hline
    Primary actor & Administrator. \\ \hline
    Secondary actor & AI verification service (provides the verdict). \\ \hline
    Pre-condition & An entity carries an AI verdict or has been flagged for review. \\ \hline
    Nominal scenario & 1.~The administrator opens the moderation panel. \newline 2.~The administrator reviews the AI verdict and rationale on the flagged entity. \newline 3.~The administrator approves or rejects, overriding the AI; \texttt{adminReviewed} is set and the decision is final. \newline 4.~For a violating store, the administrator suspends or deletes it, optionally with a reason. \newline 5.~The system updates the status and writes a \texttt{StoreStatusLog} entry, cascading to products on delete. \\ \hline
    Alternative scenario & A1.~The administrator agrees with the AI: no override is needed and the verdict stands. \newline A2.~Reactivate a suspended store: its status returns to \textsc{active}. \\ \hline
    Post-condition & The entity's \texttt{adminReviewed} flag is set and its verification or store status reflects the administrator's final decision. \\ \hline
  \end{tabularx}
\end{table}

Figure~\ref{fig:act-moderation} models the administrator's
content-moderation decision over a flagged entity.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/act-moderation.png}
  \caption{Activity diagram, content moderation.}
  \label{fig:act-moderation}
\end{figure}

\subsection{n8n Workflow Design}

The platform's AI features are carried by four independent n8n workflows
deployed on the same engine, each exposed as an HTTP webhook the backend
invokes. The \textbf{verification} workflow (Figure~\ref{fig:n8n-verify})
fans the image list out into one branch per image, downloads each file,
moderates it against the marketplace rules with a vision model, and posts
the aggregated verdict back to the callback. The \textbf{database chat}
workflow (Figure~\ref{fig:n8n-chat}) wraps an AI agent, with a chat
model, conversation memory and a SQL execution tool, that answers an
administrator's natural-language question by querying the database. The
\textbf{storefront-redesign} workflow (Figure~\ref{fig:n8n-redesign}),
already detailed in Sprint~2, builds a prompt, calls the language model,
and parses and sanitises the returned customisation object. The
\textbf{suggestion} workflow (Figure~\ref{fig:n8n-suggest}) takes a viewed
product, reasons over the catalogue through a read-only SQL tool, and
returns, through a structured-output parser that enforces a
\texttt{\{betterDeal, alternatives\}} shape, the identifiers of a
cheaper same item and a few substitute alternatives, which the backend
validates before serving (detailed below).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-verify.png}
  \caption{n8n workflow, AI verification.}
  \label{fig:n8n-verify}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-chat.png}
  \caption{n8n workflow, database chat assistant.}
  \label{fig:n8n-chat}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-redesign.png}
  \caption{n8n workflow, AI storefront redesign.}
  \label{fig:n8n-redesign}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-product-suggestions.png}
  \caption{n8n workflow, product suggestions.}
  \label{fig:n8n-suggest}
\end{figure}

\subsection{Product Suggestions}

On every product page the platform surfaces two tiers of cross-store
suggestion, a Tier~1 ``same item, cheaper'' offer and a Tier~2 set of
substitute \emph{alternatives}, distinct from the tag-based
landing-page recommendations. They are produced \textbf{live, on each
product view} rather than precomputed: an initial design that stored a
vector embedding per product and compared them in-app was deliberately
dropped in favour of the live workflow, so a newly listed product is
eligible immediately and no stored data can go stale. The frontend
requests them on a separate asynchronous query, so the product page paints
at once and only the suggestion section waits.

The endpoint \texttt{GET /product/:id/suggestions} calls the suggestion
workflow (Figure~\ref{fig:n8n-suggest}) with the viewed product, bounded by
a timeout, and receives a structured \texttt{\{betterDeal, alternatives\}}
list of product ids. Because that output originates from a language model,
it crosses a trust boundary and is \textbf{validated deterministically at
serve time}: every id is hydrated and dropped unless it is approved, not
banned, available, in stock and different from the viewed product, and the
better deal is kept only when its price is genuinely below the viewed one.

On any workflow error, timeout, or empty or malformed response the backend
falls back to a \textbf{deterministic catalogue query}: eligible products
in the same category, ranked by a similarity score that weighs shared
product-name tokens most, then shared tags, then the same sub-category,
with popularity breaking ties. The fallback guarantees the section is
never empty on a transient failure, while the live workflow, when
available, adds semantic reasoning over the product name and
description that a rigid query cannot. Figure~\ref{fig:seq-suggestions}
traces the request from the product page through the workflow call,
serve-time validation, and the fallback.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Sequence diagram, the product page requests suggestions; the backend calls the workflow, validates and hydrates the returned ids, and falls back to the deterministic catalogue query on any failure}
  \caption{Sequence diagram, product-suggestions request, serve-time validation and fallback.}
  \label{fig:seq-suggestions}
\end{figure}

\subsection{Premium Tier}

Unlike the one-time checkout of Sprint~3, the premium tier is a
\emph{recurring monthly subscription}, and it therefore introduces two
Stripe mechanisms beyond that sprint's single PaymentIntent:
\textbf{Stripe Checkout in subscription mode} for sign-up and the
\textbf{Stripe Customer Portal} for self-service management. A seller
starts the flow from the upgrade page; the backend creates, or reuses
, a Stripe \emph{customer} for the account, stamps its identifier on
\texttt{stripeCustomerId}, and opens a hosted Checkout session in
subscription mode for the configured monthly price. Everything afterwards
, cancelling, updating the card, downloading invoices, is delegated
to the hosted Customer Portal rather than rebuilt in-house.

The tier is \textbf{sellers-only}: the \texttt{premium/checkout} endpoint
is role-gated to the seller role, a non-seller is routed instead to the
become-a-seller flow, and administrators keep a \textbf{manual grant} comp
path that marks an account premium without any Stripe subscription.

Entitlement is \textbf{webhook-driven}. The subscription lifecycle events
, \texttt{customer.subscription.created}, \texttt{...updated} and
\texttt{...deleted}, arrive through the \emph{same} signature-verified
\texttt{/payments/webhook} introduced in Sprint~3. A created or updated
subscription sets \texttt{isPremium} and stamps \texttt{premiumExpiresAt}
with the current period end, so each successful renewal simply advances
the expiry; a scheduled cancellation is recorded in
\texttt{cancelAtPeriodEnd} without revoking access, so premium is kept
until the paid period actually ends; a deleted subscription revokes
premium and detaches the subscription id. To carry this, the
\texttt{User} entity gains \texttt{stripeCustomerId},
\texttt{stripeSubscriptionId} and \texttt{cancelAtPeriodEnd} alongside the
existing \texttt{isPremium} and \texttt{premiumExpiresAt}
(Figure~\ref{fig:class-release2}); the premium-status read returned to the
client now also exposes \texttt{cancelAtPeriodEnd} and a
\texttt{manageable} flag that tells a real subscriber, who can open the
portal, apart from an administrator comp grant.

Premium unlocks the full storefront customisation set, free-form
colours, all fonts and radii, banner controls and the rest, and the
ability to operate several stores. As before, the design deliberately
\emph{preserves} a seller's premium customisation values after expiry: the
stored design remains visible to buyers and only the editing of premium
fields is locked, so a lapsed subscription never degrades a storefront
that is already live (US27).

\subsection{Realisation}

The sprint delivers the interfaces shown below. Each frame is a
placeholder for the corresponding screenshot of the running application.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{AI verification panel, entity verdict, rationale and admin override controls}
  \caption{AI verification and moderation panel.}
  \label{fig:real-aiverify}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{AI chat assistant, natural-language database query and formatted answer}
  \caption{AI database-chat assistant.}
  \label{fig:real-aichat}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Product page, the ``Same item, cheaper'' offer and the ``Alternatives'' row across stores, with the brief loading state shown while the live suggestions are fetched}
  \caption{Cross-store product suggestions on the product page.}
  \label{fig:real-suggestions}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Upgrade page, the premium plan with the subscribe call to action, and, for an active subscriber, the manage-subscription controls (cancel at period end, update card, invoices)}
  \caption{Premium upgrade and subscription-management page.}
  \label{fig:real-upgrade}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Appearance, the same page in the light theme and in the dimmed dark theme, with the theme switch open in the navigation bar}
  \caption{Light and dimmed-dark application themes.}
  \label{fig:real-theme}
\end{figure}

Across both themes the application carries a deliberate visual identity,
applied entirely through CSS design tokens: a \textbf{warm-coral} brand
accent in the light theme, a dimmed dark theme for low-light use, and
\textbf{Inter} as the interface typeface. Centralising colour, typography
and radius as tokens lets every component re-theme instantly and keeps the
brand consistent across the application chrome, while seller storefronts
intentionally retain their own chosen palette.

\section{Tests \& Validation}
\label{sec:tests}

Quality is enforced by an automated suite of \textbf{538 tests} that runs
on every push and gates every deployment.

\subsection{Testing Strategy and Tooling}

The backend is tested with \textbf{Jest~30}~\cite{jest} and \texttt{ts-jest}, which
transforms TypeScript on the fly and runs every co-located
\texttt{*.spec.ts} file; collaborators such as the Prisma service, the
notification service, the SMTP mailer and the authentication guards are
replaced with deterministic mocks. The frontend is tested with
\textbf{Vitest}~\cite{vitest} and React Testing Library with \texttt{jest-dom}, which
mount components, query the DOM by accessible role and text, and assert
on the rendered output. Good tests here verify observable behaviour, 
the result of a service call, the response of an endpoint, the rendered
state of a component, rather than internal implementation details, so
they survive refactoring.

\subsection{Test Pyramid}

The suite follows the classical pyramid: a wide base of fast pure-logic
and frontend tests, a thicker middle of service-level tests that run the
real service code against a mocked Prisma client, and a narrow top of
integration tests that exercise the wired-up HTTP stack through
supertest and the global deny-by-default guard.
Figure~\ref{fig:test-pyramid} shows how the tests distribute across these
layers.

\begin{figure}[H]
  \centering
  \begin{minipage}{0.85\textwidth}
  \centering
  \texttt{\begin{tabular}{c}
    \textbf{Integration (85 tests)} \\
    \footnotesize Nest app + ValidationPipe + supertest + global guard \\
    \footnotesize controllers across the modules; deny-by-default guard wiring \\[4pt]
    \hline\\[-6pt]
    \textbf{Service unit tests (253 tests)} \\
    \footnotesize Real service code, mocked Prisma \\
    \footnotesize Product, Store, Order, Payment, Subscription, User, Notification, \ldots \\[4pt]
    \hline\\[-6pt]
    \textbf{Pure-logic + frontend (200 tests)} \\
    \footnotesize No I/O, no framework wiring \\
    \footnotesize \texttt{validate-env}, \texttt{tndToStripeUsdMinor}, guards; stores, hooks, components \\
  \end{tabular}}
  \end{minipage}
  \caption{Test pyramid, 538 tests across backend (Jest, 391) and frontend (Vitest, 147).}
  \label{fig:test-pyramid}
\end{figure}

\subsection{Backend Unit and Integration Tests}

Backend unit tests inject an in-memory mock of the Prisma service into
the real service under test through the NestJS \texttt{TestingModule},
which lets each test drive arbitrary return values and assert on the
exact arguments passed to Prisma, validating business rules such as
ownership enforcement, the server-side payment-amount derivation and the
stale-checkout reconciliation. The product-suggestions logic is covered
the same way: the serve-time validation and hydration, the deterministic
fallback ranking and the structured-output parsing are unit-tested against
mocked workflow responses, and the endpoint is exercised with the
suggestion webhook mocked so the happy path, the eligibility drops and the
fallback are all asserted without any network call. Integration tests boot
the Nest application with its validation pipe and global guard and drive
real controllers through supertest, confirming that the deny-by-default
authorisation wiring admits exactly the intended routes.

\subsection{Frontend Tests}

The frontend tests cover the Zustand stores, the custom hooks and the
React components, including the theme switch and the centralised
status-badge helper introduced with the light/dark theming, mounting
them in a jsdom environment and asserting on accessible, user-visible
output. Together with the backend suite they give the layered coverage
summarised in the pyramid above.

\subsection{Code Coverage and Continuous Integration}

Both runners collect coverage, and the whole suite is wired into the
continuous-integration pipeline: every push runs linting, a build and the
full test run for the backend and the frontend, and a red suite blocks
the build-and-deploy jobs that follow (Section~\ref{sec:cicd}).
Figure~\ref{fig:real-ci} stands in for the CI run and coverage summary.

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Continuous-integration run, test and coverage summary for backend and frontend}
  \caption{Continuous-integration test and coverage summary.}
  \label{fig:real-ci}
\end{figure}

\section{DevOps \& Deployment}

The platform is delivered as a set of containers, provisioned on
reproducible infrastructure and shipped by an automated pipeline. The
logical and physical architecture were established in
Section~\ref{chap:requirements}; this section details the realisation of
the delivery chain.

\subsection{Containerisation}

Each service ships as a Docker image. The backend uses a
\textbf{multi-stage} build (Listing~\ref{lst:dockerfile}): a builder
stage installs dependencies, generates the Prisma client and compiles
the application, then prunes development dependencies, and a slim
production stage copies only the build artefacts and runs as the
unprivileged \texttt{node} user, applying database migrations on start.

\begin{lstlisting}[caption={Backend multi-stage Dockerfile (excerpt).},label={lst:dockerfile}]
# build stage
FROM node:20-alpine AS builder
RUN corepack enable && corepack prepare pnpm@10 --activate
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile
COPY . .
RUN npx prisma generate && pnpm run build && pnpm prune --prod

# production stage
FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/prisma ./prisma
USER node
EXPOSE 3000
CMD ["sh", "-c", "npx prisma migrate deploy && node dist/src/main"]
\end{lstlisting}

The services are composed with Docker Compose: a MariaDB database with a
health check, the backend, the frontend, an n8n engine (with a one-shot
initialiser that imports the version-controlled workflows), and a
\textbf{Caddy}~\cite{caddy} reverse proxy that terminates TLS with automatically
renewed Let's Encrypt certificates and adds the HTTP security headers.
Listing~\ref{lst:compose} shows the backend and proxy service blocks.

\begin{lstlisting}[caption={Docker Compose backend and reverse-proxy services (excerpt).},label={lst:compose}]
services:
  backend:
    image: registry/marketplace-backend:${IMAGE_TAG:-latest}
    restart: always
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      NODE_ENV: production
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      N8N_VERIFY_WEBHOOK_URL: http://n8n:5678/webhook/ai-verify
    volumes:
      - uploads:/app/uploads

  caddy:
    image: caddy:2-alpine
    restart: always
    ports: ["80:80", "443:443"]
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
\end{lstlisting}

\subsection{Infrastructure as Code}

The production environment is described declaratively with
\textbf{Terraform} using the DigitalOcean~\cite{digitalocean} provider, so the whole server
can be recreated identically with a single \texttt{terraform apply} and
every change is reviewable as a diff. Three resources define the
environment: a private \texttt{digitalocean\_vpc}, an
\texttt{ubuntu-24-04-x64} \texttt{digitalocean\_droplet} attached to it
with the operator's SSH key, and a firewall configured through the
droplet's \texttt{user\_data} cloud-init script. That script makes the
first boot zero-touch: it installs Docker Engine and the Compose plugin,
then configures UFW to \emph{deny all inbound by default} while allowing
only SSH, HTTP, HTTPS and the n8n webhook port. The provider also manages
the DNS records that point the domain at the droplet.

\subsection{CI/CD Pipeline}
\label{sec:cicd}

A \textbf{GitHub Actions}~\cite{githubactions} workflow turns a push to \texttt{main} into a
live deployment (Listing~\ref{lst:deploy}). It first runs the backend and
frontend test jobs; only if both pass does it provision the
infrastructure with Terraform, build and push the backend and frontend
Docker images to the registry, and finally deploy by copying the Compose
file to the droplet over SSH and bringing the stack up. Secrets are
injected from the CI secret store, so no credential lives in the
repository, and DNS is updated to the droplet's address automatically.

\begin{lstlisting}[caption={GitHub Actions build-and-deploy pipeline (excerpt).},label={lst:deploy}]
name: Build, Push & Deploy
on:
  push:
    branches: [main]

jobs:
  test-backend:    # pnpm install, prisma generate, lint, build, jest
    runs-on: ubuntu-latest
  test-frontend:   # pnpm install, lint, build, vitest
    runs-on: ubuntu-latest
  provision:       # terraform init + apply (droplet, firewall, DNS)
    runs-on: ubuntu-latest
  build-backend:   # docker build + push to the registry
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
  deploy:
    needs: [provision, build-backend, build-frontend]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        run: ssh $HOST "docker compose pull && docker compose up -d"
\end{lstlisting}

\section*{Conclusion}

Release~2 completed the product. Sprint~3 made the marketplace
transactable, a multi-store cart and checkout, a server-authoritative
Stripe flow confirmed by a signed webhook, self-service account
management, and administration and moderation tooling. Sprint~4 added the
intelligence and delivery layers, automated AI verification with
administrative override, the database chat assistant, the premium tier,
light and dimmed-dark theming, a 538-test suite, and a containerised,
Terraform-provisioned deployment driven by a GitHub Actions pipeline. The
marketplace planned in the first two chapters is now fully realised,
validated and deployable.
