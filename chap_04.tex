\chapter{Release~2: Transactions, Intelligence \& Delivery}
\label{chap:release2}

\section*{Introduction}

This chapter presents the second release, \textit{Transactions,
Intelligence \& Delivery}, which turns the populated marketplace of
Release~1 into a transacting, self-moderating and deployable product. It
opens with a release overview --- a release-scoped use-case diagram and
the subset of the domain model touched by the release --- followed by
the release backlog. It then presents the two sprints of the release at
the same depth as Chapter~\ref{chap:release1}. \textbf{Sprint~3 (Orders,
Payment \& Administration)} delivers the cart, the multi-store checkout,
the Stripe payment flow and the administration tooling; \textbf{Sprint~4
(AI Verification \& Premium)} delivers the automated AI pipeline, the
premium tier, and --- folded into the same sprint --- the testing and
the DevOps and deployment that bring the platform to production. The
present chapter details Sprint~3 first.

\section{Release Overview}

Release~2 completes the marketplace over two sprints. Sprint~3 makes the
catalogue transactable: customers fill a cart, check out across multiple
stores in a single order, and pay through Stripe, while administrators
moderate stores and products, curate featured content and manage
categories. Sprint~4 adds the intelligence and delivery layers: automated
AI verification of new stores and products, the AI database-chat
assistant, the premium subscription tier, a light/dark theming option,
and the test suite and
deployment pipeline. As before, the architecture and technology stack of
Section~\ref{chap:requirements} are not repeated; this release
instantiates them for the transactional, intelligence and delivery
features.

\subsection{Release Use-Case Diagram}

Figure~\ref{fig:uc-release2} scopes the use cases delivered by
Release~2. Customers gain the cart, checkout and payment; sellers gain
order visibility, the premium tier and AI redesign; administrators gain
moderation, featured curation, category management and the authority to
override AI verdicts; the AI service and the Stripe payment gateway act
as secondary actors.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.88\textheight,keepaspectratio]{img/uc-release2.png}
  \caption{Release~2 use-case diagram (transactions, intelligence and delivery).}
  \label{fig:uc-release2}
\end{figure}

\subsection{Release Domain Model}

Figure~\ref{fig:class-release2} shows the slice of the domain model
exercised by Release~2: the \texttt{Order} and its \texttt{OrderItem}s,
the payment fields stamped on the order, the verification flags on
\texttt{Store} and \texttt{Product}, the \texttt{StoreStatusLog} audit
trail and the featured-content entities.

\begin{figure}[H]
  \centering
  \includegraphics[width=\columnwidth,keepaspectratio]{img/class-release2.png}
  \caption{Release~2 domain model (subset of the platform class diagram).}
  \label{fig:class-release2}
\end{figure}

\section{Release~2 Backlog}

The release backlog draws the remaining user stories from the product
backlog of Section~\ref{chap:requirements} --- including the payment
story US36, the account-management stories US38--US39 and the theming
story US40 --- and allocates them to the two sprints, as summarised in
Table~\ref{tab:backlog-r2}.

\begin{table}[H]
  \centering
  \caption{Release~2 backlog --- user stories grouped by sprint and feature.}
  \label{tab:backlog-r2}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{|l|l|X|c|}
    \hline
    \rowcolor{tableHeader}
    \textcolor{white}{\textbf{Sprint}} & \textcolor{white}{\textbf{Feature}} &
    \textcolor{white}{\textbf{User stories}} & \textcolor{white}{\textbf{Pts}} \\
    \hline
    \multirow{3}{*}{Sprint 3} & Order System \& Payment & US14--US17, US36 & 11 \\ \cline{2-4}
                              & Account Management      & US38, US39       & 3 \\ \cline{2-4}
                              & Administration          & US19--US21       & 4 \\ \hline
    \multirow{4}{*}{Sprint 4} & AI Verification         & US22--US24, US35 & 8 \\ \cline{2-4}
                              & Premium Tier            & US25--US27       & 8 \\ \cline{2-4}
                              & Appearance \& Theming   & US40             & 2 \\ \cline{2-4}
                              & CI/CD \& Deployment     & US28--US33       & 13 \\ \hline
    \multicolumn{3}{|r|}{\textbf{Release~2 total}} & \textbf{49} \\ \hline
  \end{tabularx}
\end{table}

\section{Sprint 3 --- Orders, Payment \& Administration}

\subsection{Sprint Objective}

Sprint~3 makes the marketplace transactable and governable. By the end of
the sprint a customer can build a cart, check out across several stores
in a single order, and pay securely through Stripe, receiving a
confirmation email; a seller can see the orders that include their
products; every signed-in user can manage their own account --- viewing
and editing their profile, changing their password and reviewing their
purchase history --- and an administrator can moderate stores and
products, suspend or delete a store, curate featured content and manage
the category tree.
The payment flow is the centrepiece: amounts are computed server-side,
charged through a single Stripe~\cite{stripe} PaymentIntent, and confirmed
asynchronously by a signature-verified webhook.

\subsection{Sprint Use-Case Diagram}

Figure~\ref{fig:uc-sprint3} details the sprint's use cases. Checkout
\emph{includes} the secure payment, which in turn drives the Stripe
gateway and the confirmation email; suspending a store is an
\emph{extension} of store moderation.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\columnwidth,keepaspectratio]{img/uc-sprint3.png}
  \caption{Sprint~3 use-case diagram (orders, payment and administration).}
  \label{fig:uc-sprint3}
\end{figure}

\subsection{Analysis and Design}

The five central use cases of the sprint are specified textually, then
the checkout-and-payment control flow is given as an activity diagram and
the order placement as a sequence diagram. The Stripe payment mechanism
is then described in its own section.

\subsubsection{Textual Use-Case Descriptions}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Checkout} use case.}
  \label{tab:uc-checkout}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Checkout}}} \\
    \hline
    Actor & Customer (authenticated, verified). \\ \hline
    Objective & Convert the cart into an order, even when it spans several stores. \\ \hline
    Pre-conditions & The cart is non-empty and the customer is signed in with a verified email. \\ \hline
    Post-conditions & A single \textsc{pending} \texttt{Order} is created with one \texttt{OrderItem} per product line across all stores, and stock is reserved. \\ \hline
    Nominal scenario & 1.~The customer opens the checkout page. \newline 2.~The customer reviews the items, quantities and shipping form. \newline 3.~The customer confirms. \newline 4.~The server prices each line server-side (using the sale price when a sale is active), reserves stock, and creates one order spanning all stores with its order items. \newline 5.~The payment step opens. \\ \hline
    Alternative scenarios & A1.~Insufficient stock on a line: the line is flagged and checkout halts. \newline A2.~Unverified email: checkout is blocked until verification. \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Pay securely} use case.}
  \label{tab:uc-pay}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Pay securely}}} \\
    \hline
    Actor & Customer; Payment Gateway (Stripe, secondary). \\ \hline
    Objective & Pay the order total securely and have the order confirmed. \\ \hline
    Pre-conditions & A \textsc{pending} order with a positive total and reserved stock exists. \\ \hline
    Post-conditions & A Stripe PaymentIntent is opened for the server-computed total; on a signature-verified success webhook the order is marked \textsc{paid} and \textsc{confirmed} and a confirmation email is sent; on failure the reserved stock is released. \\ \hline
    Nominal scenario & 1.~The server computes the grand total and converts it from TND to USD minor units. \newline 2.~The server opens one PaymentIntent for the total with an idempotency key and stamps its id on the order. \newline 3.~The client confirms the payment with Stripe. \newline 4.~Stripe sends a signed webhook; the backend verifies the signature against the raw body. \newline 5.~On \texttt{payment\_intent.succeeded} the order becomes \textsc{paid} and \textsc{confirmed} and the confirmation email is sent. \\ \hline
    Alternative scenarios & E1.~Payment fails or is canceled: the order is marked failed and the reserved stock is released (idempotently). \newline E2.~Lost webhook: a periodic reconciliation sweep retrieves the intent from Stripe and settles or restocks the order. \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Manage profile} use case.}
  \label{tab:uc-profile}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Manage profile}}} \\
    \hline
    Actor & Any authenticated user (Customer, Seller or Administrator). \\ \hline
    Objective & View and update one's own account: display name, phone and avatar, change the password, and review the purchase history. \\ \hline
    Pre-conditions & The user is signed in. \\ \hline
    Post-conditions & The editable fields (name, phone, avatar) are updated; a password change revokes the other sessions; privileged fields (role, premium status, email) are never altered through this surface. \\ \hline
    Nominal scenario & 1.~The user opens their account page (a customer through the tabbed account page, a seller through the dashboard). \newline 2.~The user edits the name, phone or avatar and saves; the server accepts only those fields. \newline 3.~The user optionally changes the password by supplying the current and a new one. \newline 4.~The user opens the purchase-history view to consult past orders, paginated. \\ \hline
    Alternative scenarios & A1.~A privileged field is submitted: the server ignores it, so a user can never self-grant a role or premium status. \newline E1.~Invalid input (e.g.\ a too-short new password or a mismatched confirmation): validation rejects the change with a message. \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Suspend store} use case.}
  \label{tab:uc-suspend}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Suspend store}}} \\
    \hline
    Actor & Administrator. \\ \hline
    Objective & Suspend or delete a store that violates the marketplace rules. \\ \hline
    Pre-conditions & The target store exists. \\ \hline
    Post-conditions & The store \texttt{currentStatus} becomes \textsc{suspended} (or \textsc{deleted}, cascade-removing its products); a \texttt{StoreStatusLog} entry records the change; the store leaves public browsing. \\ \hline
    Nominal scenario & 1.~The administrator opens the store moderation panel. \newline 2.~The administrator selects a store and inspects it. \newline 3.~The administrator chooses suspend or delete, optionally with a reason. \newline 4.~The system updates the status, writes a \texttt{StoreStatusLog} entry, and on delete cascades to the store's products. \\ \hline
    Alternative scenarios & A1.~Reactivate a suspended store: \texttt{currentStatus} returns to \textsc{active}. \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Override AI verdict} use case.}
  \label{tab:uc-override}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Override AI verdict}}} \\
    \hline
    Actor & Administrator. \\ \hline
    Objective & Override the automated verification verdict on a store or product. \\ \hline
    Pre-conditions & The entity carries an AI verdict (\texttt{aiReviewed = true}) or has been flagged. \\ \hline
    Post-conditions & The entity's \texttt{adminReviewed} flag is set and its verification status reflects the administrator's decision, which takes precedence over the AI verdict. \\ \hline
    Nominal scenario & 1.~The administrator opens the moderation panel. \newline 2.~The administrator reviews the AI verdict and rationale on the flagged entity. \newline 3.~The administrator approves or rejects, overriding the AI. \newline 4.~The system sets \texttt{adminReviewed} and updates the verification status; the administrator's decision is final. \\ \hline
    Alternative scenarios & A1.~The administrator agrees with the AI: no override is needed and the AI verdict stands. \\ \hline
  \end{tabularx}
\end{table}

\subsubsection{Activity Diagram}

Figure~\ref{fig:act-checkout-payment} models the checkout-and-payment
control flow. After stock is reserved and the pending order priced
server-side, a single Stripe PaymentIntent is opened; the signed webhook
then drives the order either to \textsc{paid}/\textsc{confirmed} on
success or back to released stock on failure.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.82\textheight,keepaspectratio]{img/act-checkout-payment.png}
  \caption{Activity diagram --- multi-store checkout and Stripe payment.}
  \label{fig:act-checkout-payment}
\end{figure}

\subsubsection{Sequence Diagram}

Figure~\ref{fig:seq-order} traces order placement across the React
application, the NestJS API and the database, including the creation of
the single multi-store order and its items.

\begin{figure}[H]
  \centering
  \includegraphics[width=\columnwidth,keepaspectratio]{img/seq-order.png}
  \caption{Sequence diagram --- multi-store order placement.}
  \label{fig:seq-order}
\end{figure}

\subsection{Stripe Payment Design}

Payment is the most security-sensitive flow of the platform, and its
design follows a single principle: \textbf{the client never dictates what
it is charged}. When the customer checks out, the backend reserves stock,
creates the pending order(s) for the cart, and computes the grand total
from the server-side prices. Stripe accounts cannot settle in Tunisian
dinar, so the dinar total is converted to United States dollar minor
units through a configurable rate (\texttt{TND\_TO\_USD\_RATE}) while the
interface continues to display dinar. A single PaymentIntent is then
opened for the whole cart --- even when it spans several stores --- with
an \textbf{idempotency key} derived from the order identifiers, so that a
retried request never creates a duplicate charge. The PaymentIntent
identifier is stamped on the order(s) for later reconciliation.
Listing~\ref{lst:paymentintent} shows the core of this server-side
charge.

\begin{lstlisting}[caption={Server-side PaymentIntent creation (amount derived on the server).},label={lst:paymentintent}]
const intent = await stripe.paymentIntents.create(
  {
    // amount is computed server-side and converted TND -> USD minor units
    amount: tndToStripeUsdMinor(grandTotal),
    currency: 'usd',
    metadata: {
      orderIds: orderIds.join(','),
      displayCurrency: 'TND',
      displayAmount: grandTotal.toFixed(3),
    },
  },
  { idempotencyKey: `checkout-${orderIds.join('-')}` },
);
await orderService.setPaymentIntent(orderIds, intent.id);
\end{lstlisting}

Confirmation is asynchronous. Stripe calls an \textbf{unauthenticated}
\texttt{POST /payments/webhook} endpoint --- it is deliberately public,
because the caller is Stripe rather than a logged-in user. Trust is
established not by a session but by \textbf{verifying the Stripe
signature against the raw request body}; an unverifiable payload is
rejected. On a \texttt{payment\_intent.succeeded} event the order(s) are
marked \textsc{paid} and \textsc{confirmed} and the confirmation email is
sent; on a failed or canceled event the reserved stock is released. The
handler is idempotent, so a duplicated webhook is harmless. Finally, a
periodic cleanup sweep reconciles checkouts left unpaid past a staleness
window: it retrieves each PaymentIntent from Stripe and either settles a
paid-but-unacknowledged order or releases the stock of an abandoned one,
so a lost webhook can never strand stock or a payment.

\subsection{Realisation}

The sprint delivers the transaction and administration interfaces shown
below. Each frame is a placeholder for the corresponding screenshot of
the running application.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Checkout page --- multi-store cart summary, shipping form and Stripe payment element}
  \caption{Checkout page with Stripe payment.}
  \label{fig:real-checkout}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Account page --- profile view/edit (name, phone, avatar), change-password form and the paginated purchase history}
  \caption{User account and profile page.}
  \label{fig:real-profile}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Administrator dashboard --- marketplace metrics and navigation}
  \caption{Administrator dashboard.}
  \label{fig:real-admindash}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Moderation panel --- stores and products with suspend / delete and verdict-override actions}
  \caption{Store and product moderation panel.}
  \label{fig:real-moderation}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Category management --- category and sub-category tree}
  \caption{Category management.}
  \label{fig:real-categories}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Featured-content curation --- selecting featured stores and products for the landing page}
  \caption{Featured-content curation.}
  \label{fig:real-featured}
\end{figure}

\section{Sprint 4 --- AI Verification \& Premium Tier}

\subsection{Sprint Objective}

Sprint~4 adds the intelligence and monetisation layers. Every new store
and product is screened automatically by an AI verification pipeline
before it becomes visible; suspicious content is flagged for the
administrator, who remains the final authority. Administrators gain a
natural-language database chat assistant for diagnostics, and sellers can
subscribe to a premium tier --- reusing the Stripe machinery of Sprint~3
--- that unlocks the full customisation set and multiple stores. The AI
capabilities are delivered through three independent n8n workflows
invoked by the backend over HTTP. Finally, the sprint rounds out the user
experience with an appearance option: a light and a dimmed dark theme,
chosen explicitly or following the operating-system preference and
applied consistently across the application chrome through CSS design
tokens.

\subsection{Sprint Use-Case Diagram}

Figure~\ref{fig:uc-sprint4} details the sprint's use cases. Automatic
verification \emph{includes} returning a verdict with a rationale and may
\emph{extend} into flagging content for the administrator to override;
subscribing to premium \emph{unlocks} the advanced customisation and
multiple stores through the Stripe gateway.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\columnwidth,keepaspectratio]{img/uc-sprint4.png}
  \caption{Sprint~4 use-case diagram (AI verification and premium tier).}
  \label{fig:uc-sprint4}
\end{figure}

\subsection{Analysis and Design}

The four central use cases of the sprint are specified textually, then
the verification and moderation control flows are given as activity
diagrams and the verification exchange as a sequence diagram.

\subsubsection{Textual Use-Case Descriptions}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{AI verification} use case.}
  \label{tab:uc-aiverify}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: AI verification}}} \\
    \hline
    Actor & AI Service (n8n + vision LLM); triggered by store or product creation. \\ \hline
    Objective & Automatically screen every new store and product before it becomes visible. \\ \hline
    Pre-conditions & A new entity exists with \texttt{aiReviewed = false} and \texttt{verificationStatus = pending}, and its images are uploaded. \\ \hline
    Post-conditions & \texttt{aiReviewed} becomes \texttt{true} and the verification status is set to \textsc{approved} (auto-published) or the entity is flagged for administrator review; a verdict and rationale are recorded. \\ \hline
    Nominal scenario & 1.~After image upload, the backend fires the \texttt{ai-verify} webhook with the listing text and image list. \newline 2.~n8n downloads each image and submits it with the text to the vision model. \newline 3.~The per-image verdicts are aggregated, flagging the entity if any image is flagged. \newline 4.~The decision is posted to the \texttt{/ai-review} callback. \newline 5.~The backend publishes clean entities and queues flagged ones for moderation. \\ \hline
    Alternative scenarios & E1.~The webhook or model fails: the entity stays \textsc{pending} and is surfaced in the administrator's queue for manual review. \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{AI chat query} use case.}
  \label{tab:uc-aichat}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: AI chat query}}} \\
    \hline
    Actor & Administrator; AI Service (n8n + LLM + SQL tool). \\ \hline
    Objective & Query the database in natural language for diagnostics and insight. \\ \hline
    Pre-conditions & The administrator is authenticated. \\ \hline
    Post-conditions & A natural-language answer, derived from a generated SQL query executed read-only against the database, is returned; no data is modified. \\ \hline
    Nominal scenario & 1.~The administrator types a question in the chat assistant. \newline 2.~The backend forwards it to the n8n chat workflow. \newline 3.~The AI agent translates the question into SQL, executes it against MariaDB through the SQL tool, and formats the result. \newline 4.~The answer is returned to the administrator. \\ \hline
    Alternative scenarios & A1.~An ambiguous question: the assistant asks for clarification, using its conversation memory for context. \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Subscribe to premium} use case.}
  \label{tab:uc-premium}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Subscribe to premium}}} \\
    \hline
    Actor & Seller; Payment Gateway (Stripe, secondary). \\ \hline
    Objective & Unlock the premium tier: full storefront customisation and multiple stores. \\ \hline
    Pre-conditions & The user holds the seller role. \\ \hline
    Post-conditions & After a successful Stripe payment, \texttt{isPremium} becomes \texttt{true} and \texttt{premiumExpiresAt} is set to a future date; premium customisation fields and multi-store operation are unlocked. \\ \hline
    Nominal scenario & 1.~The seller opens the premium plan. \newline 2.~The seller pays through the same Stripe machinery used at checkout. \newline 3.~On payment success the account is marked premium with an expiry date. \newline 4.~The premium controls unlock. \\ \hline
    Alternative scenarios & A1.~The subscription expires: premium fields lock for further editing, but the existing premium customisation values are preserved and remain visible to buyers (US27). \\ \hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Textual description of the \emph{Switch theme} use case.}
  \label{tab:uc-theme}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3cm}|X|}
    \hline
    \rowcolor{tableHeader}
    \multicolumn{2}{|l|}{\textcolor{white}{\textbf{Use case: Switch theme}}} \\
    \hline
    Actor & Any user, whether authenticated or not. \\ \hline
    Objective & Choose how the application is rendered: a light theme, a dimmed dark theme, or automatically following the operating-system preference. \\ \hline
    Pre-conditions & None; the theme switch is reachable from any page through the navigation chrome. \\ \hline
    Post-conditions & The interface re-themes immediately and the choice is persisted on the device, so it is re-applied before first paint on later visits. \\ \hline
    Nominal scenario & 1.~The user opens the theme switch and picks light, dark or system. \newline 2.~The interface re-themes at once through CSS design tokens, with no page reload. \newline 3.~The preference is stored locally and restored on the next visit before the first paint, so there is no flash of the wrong theme. \\ \hline
    Alternative scenarios & A1.~The user picks \emph{system}: the interface follows the OS light/dark setting and updates live when that setting changes. \newline A2.~A seller storefront keeps its own seller-chosen palette and is intentionally not overridden by the application theme. \\ \hline
  \end{tabularx}
\end{table}

\subsubsection{Activity Diagrams}

Figure~\ref{fig:act-ai-verification} models the AI verification pipeline,
including the per-image screening loop inside the n8n workflow and the
branch that either publishes a clean entity or routes a flagged one to
moderation. Figure~\ref{fig:act-moderation} models the administrator's
content-moderation decision over a flagged entity.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.84\textheight,keepaspectratio]{img/act-ai-verification.png}
  \caption{Activity diagram --- AI verification pipeline.}
  \label{fig:act-ai-verification}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[height=0.6\textheight,keepaspectratio]{img/act-moderation.png}
  \caption{Activity diagram --- content moderation.}
  \label{fig:act-moderation}
\end{figure}

\subsubsection{Sequence Diagram}

Figure~\ref{fig:seq-verify} traces the verification exchange for both
products and stores: creation with a pending status, image upload, the
per-image moderation loop against the vision model, the aggregated
verdict posted to the callback, and the alternative branches of
automatic approval or administrative override.

\begin{figure}[H]
  \centering
  \includegraphics[width=\columnwidth,keepaspectratio]{img/seq-verify.png}
  \caption{Sequence diagram --- AI verification with administrative override.}
  \label{fig:seq-verify}
\end{figure}

\subsection{n8n Workflow Design}

The platform's AI features are carried by three independent n8n workflows
deployed on the same engine, each exposed as an HTTP webhook the backend
invokes. The \textbf{verification} workflow (Figure~\ref{fig:n8n-verify})
fans the image list out into one branch per image, downloads each file,
moderates it against the marketplace rules with a vision model, and posts
the aggregated verdict back to the callback. The \textbf{database chat}
workflow (Figure~\ref{fig:n8n-chat}) wraps an AI agent --- with a chat
model, conversation memory and a SQL execution tool --- that answers an
administrator's natural-language question by querying the database. The
\textbf{storefront-redesign} workflow (Figure~\ref{fig:n8n-redesign}),
already detailed in Sprint~2, builds a prompt, calls the language model,
and parses and sanitises the returned customisation object.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-verify.png}
  \caption{n8n workflow --- AI verification.}
  \label{fig:n8n-verify}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-chat.png}
  \caption{n8n workflow --- database chat assistant.}
  \label{fig:n8n-chat}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,keepaspectratio]{img/n8n-redesign.png}
  \caption{n8n workflow --- AI storefront redesign.}
  \label{fig:n8n-redesign}
\end{figure}

\subsection{Premium Tier}

The premium tier is monetised through the same Stripe machinery designed
in Sprint~3, so no new payment mechanism is introduced. A successful
payment marks the account premium by setting \texttt{isPremium} and a
future \texttt{premiumExpiresAt}; premium status is evaluated by
comparing that expiry against the current time. Premium unlocks the full
storefront customisation set --- free-form colours, all fonts and radii,
banner controls and the rest --- and the ability to operate several
stores. The design deliberately \emph{preserves} a seller's premium
customisation values after expiry: the stored design remains visible to
buyers and only the editing of premium fields is locked, so a lapsed
subscription never degrades a storefront that is already live.

\subsection{Realisation}

The sprint delivers the interfaces shown below. Each frame is a
placeholder for the corresponding screenshot of the running application.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{AI verification panel --- entity verdict, rationale and admin override controls}
  \caption{AI verification and moderation panel.}
  \label{fig:real-aiverify}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{AI chat assistant --- natural-language database query and formatted answer}
  \caption{AI database-chat assistant.}
  \label{fig:real-aichat}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Appearance --- the same page in the light theme and in the dimmed dark theme, with the theme switch open in the navigation bar}
  \caption{Light and dimmed-dark application themes.}
  \label{fig:real-theme}
\end{figure}

\section{Tests \& Validation}
\label{sec:tests}

Quality is enforced by an automated suite of \textbf{479 tests} that runs
on every push and gates every deployment.

\subsection{Testing Strategy and Tooling}

The backend is tested with \textbf{Jest~30}~\cite{jest} and \texttt{ts-jest}, which
transforms TypeScript on the fly and runs every co-located
\texttt{*.spec.ts} file; collaborators such as the Prisma service, the
notification service, the SMTP mailer and the authentication guards are
replaced with deterministic mocks. The frontend is tested with
\textbf{Vitest}~\cite{vitest} and React Testing Library with \texttt{jest-dom}, which
mount components, query the DOM by accessible role and text, and assert
on the rendered output. Good tests here verify observable behaviour ---
the result of a service call, the response of an endpoint, the rendered
state of a component --- rather than internal implementation details, so
they survive refactoring.

\subsection{Test Pyramid}

The suite follows the classical pyramid: a wide base of fast pure-logic
and frontend tests, a thicker middle of service-level tests that run the
real service code against a mocked Prisma client, and a narrow top of
integration tests that exercise the wired-up HTTP stack through
supertest and the global deny-by-default guard.
Figure~\ref{fig:test-pyramid} shows how the tests distribute across these
layers.

\begin{figure}[H]
  \centering
  \begin{minipage}{0.85\textwidth}
  \centering
  \texttt{\begin{tabular}{c}
    \textbf{Integration (90 tests)} \\
    \footnotesize Nest app + ValidationPipe + supertest + global guard \\
    \footnotesize controllers across the modules; deny-by-default guard wiring \\[4pt]
    \hline\\[-6pt]
    \textbf{Service unit tests (221 tests)} \\
    \footnotesize Real service code, mocked Prisma \\
    \footnotesize Product, Store, Order, Payment, User, Settings, Notification, \ldots \\[4pt]
    \hline\\[-6pt]
    \textbf{Pure-logic + frontend (168 tests)} \\
    \footnotesize No I/O, no framework wiring \\
    \footnotesize \texttt{validate-env}, \texttt{tndToStripeUsdMinor}, guards; stores, hooks, components \\
  \end{tabular}}
  \end{minipage}
  \caption{Test pyramid --- 479 tests across backend (Jest, 346) and frontend (Vitest, 133).}
  \label{fig:test-pyramid}
\end{figure}

\subsection{Backend Unit and Integration Tests}

Backend unit tests inject an in-memory mock of the Prisma service into
the real service under test through the NestJS \texttt{TestingModule},
which lets each test drive arbitrary return values and assert on the
exact arguments passed to Prisma --- validating business rules such as
ownership enforcement, the server-side payment-amount derivation and the
stale-checkout reconciliation. Integration tests boot the Nest
application with its validation pipe and global guard and drive real
controllers through supertest, confirming that the deny-by-default
authorisation wiring admits exactly the intended routes.

\subsection{Frontend Tests}

The frontend tests cover the Zustand stores, the custom hooks and the
React components --- including the theme switch and the centralised
status-badge helper introduced with the light/dark theming --- mounting
them in a jsdom environment and asserting on accessible, user-visible
output. Together with the backend suite they give the layered coverage
summarised in the pyramid above.

\subsection{Code Coverage and Continuous Integration}

Both runners collect coverage, and the whole suite is wired into the
continuous-integration pipeline: every push runs linting, a build and the
full test run for the backend and the frontend, and a red suite blocks
the build-and-deploy jobs that follow (Section~\ref{sec:cicd}).
Figure~\ref{fig:real-ci} stands in for the CI run and coverage summary.

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Continuous-integration run --- test and coverage summary for backend and frontend}
  \caption{Continuous-integration test and coverage summary.}
  \label{fig:real-ci}
\end{figure}

\section{DevOps \& Deployment}

The platform is delivered as a set of containers, provisioned on
reproducible infrastructure and shipped by an automated pipeline. The
logical and physical architecture were established in
Section~\ref{chap:requirements}; this section details the realisation of
the delivery chain.

\subsection{Containerisation}

Each service ships as a Docker image. The backend uses a
\textbf{multi-stage} build (Listing~\ref{lst:dockerfile}): a builder
stage installs dependencies, generates the Prisma client and compiles
the application, then prunes development dependencies, and a slim
production stage copies only the build artefacts and runs as the
unprivileged \texttt{node} user, applying database migrations on start.

\begin{lstlisting}[caption={Backend multi-stage Dockerfile (excerpt).},label={lst:dockerfile}]
# build stage
FROM node:20-alpine AS builder
RUN corepack enable && corepack prepare pnpm@10 --activate
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile
COPY . .
RUN npx prisma generate && pnpm run build && pnpm prune --prod

# production stage
FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/prisma ./prisma
USER node
EXPOSE 3000
CMD ["sh", "-c", "npx prisma migrate deploy && node dist/src/main"]
\end{lstlisting}

The services are composed with Docker Compose: a MariaDB database with a
health check, the backend, the frontend, an n8n engine (with a one-shot
initialiser that imports the version-controlled workflows), and a
\textbf{Caddy}~\cite{caddy} reverse proxy that terminates TLS with automatically
renewed Let's Encrypt certificates and adds the HTTP security headers.
Listing~\ref{lst:compose} shows the backend and proxy service blocks.

\begin{lstlisting}[caption={Docker Compose --- backend and reverse-proxy services (excerpt).},label={lst:compose}]
services:
  backend:
    image: registry/marketplace-backend:${IMAGE_TAG:-latest}
    restart: always
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      NODE_ENV: production
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      N8N_VERIFY_WEBHOOK_URL: http://n8n:5678/webhook/ai-verify
    volumes:
      - uploads:/app/uploads

  caddy:
    image: caddy:2-alpine
    restart: always
    ports: ["80:80", "443:443"]
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
\end{lstlisting}

\subsection{Infrastructure as Code}

The production environment is described declaratively with
\textbf{Terraform} using the DigitalOcean~\cite{digitalocean} provider, so the whole server
can be recreated identically with a single \texttt{terraform apply} and
every change is reviewable as a diff. Three resources define the
environment: a private \texttt{digitalocean\_vpc}, an
\texttt{ubuntu-24-04-x64} \texttt{digitalocean\_droplet} attached to it
with the operator's SSH key, and a firewall configured through the
droplet's \texttt{user\_data} cloud-init script. That script makes the
first boot zero-touch: it installs Docker Engine and the Compose plugin,
then configures UFW to \emph{deny all inbound by default} while allowing
only SSH, HTTP, HTTPS and the n8n webhook port. The provider also manages
the DNS records that point the domain at the droplet.

\subsection{CI/CD Pipeline}
\label{sec:cicd}

A \textbf{GitHub Actions}~\cite{githubactions} workflow turns a push to \texttt{main} into a
live deployment (Listing~\ref{lst:deploy}). It first runs the backend and
frontend test jobs; only if both pass does it provision the
infrastructure with Terraform, build and push the backend and frontend
Docker images to the registry, and finally deploy by copying the Compose
file to the droplet over SSH and bringing the stack up. Secrets are
injected from the CI secret store, so no credential lives in the
repository, and DNS is updated to the droplet's address automatically.

\begin{lstlisting}[caption={GitHub Actions build-and-deploy pipeline (excerpt).},label={lst:deploy}]
name: Build, Push & Deploy
on:
  push:
    branches: [main]

jobs:
  test-backend:    # pnpm install, prisma generate, lint, build, jest
    runs-on: ubuntu-latest
  test-frontend:   # pnpm install, lint, build, vitest
    runs-on: ubuntu-latest
  provision:       # terraform init + apply (droplet, firewall, DNS)
    runs-on: ubuntu-latest
  build-backend:   # docker build + push to the registry
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
  deploy:
    needs: [provision, build-backend, build-frontend]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        run: ssh $HOST "docker compose pull && docker compose up -d"
\end{lstlisting}

\subsection{Production Environment}

In production the public entry point is the Caddy proxy, which terminates
HTTPS and forwards traffic to the frontend container; the frontend serves
the single-page application and proxies the API calls to the backend; the
backend talks to the MariaDB database and to the n8n engine that carries
the AI workflows. All services run under Docker Compose with restart
policies on a single DigitalOcean droplet, giving the simple,
reproducible and observable deployment targeted by the non-functional
requirements of Section~\ref{chap:requirements}.

\section*{Conclusion}

This chapter delivered Release~2 and, with it, a complete product.
Sprint~3 made the marketplace transactable --- a multi-store cart and
checkout, the server-authoritative Stripe payment flow confirmed by a
signature-verified webhook, the self-service account management that lets
every user edit their profile, change their password and review their
purchase history, and the administration tooling to moderate stores and
products, curate featured content and manage categories.
Sprint~4 added the intelligence and delivery layers --- the automated AI
verification pipeline with administrative override, the database chat
assistant, the premium tier built on the same payment machinery, a light
and dimmed-dark theming option, and the
testing and DevOps that bring the platform to production: a layered suite
of 479 tests, containerised services behind a TLS-terminating proxy,
infrastructure described with Terraform, and a GitHub Actions pipeline
that tests, builds and deploys on every push to \texttt{main}. The
marketplace planned in the first two chapters is now fully realised,
validated and deployable.
