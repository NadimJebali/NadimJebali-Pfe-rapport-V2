\chapter{Release~1: Foundations \& Core Marketplace}
\label{chap:release1}

\section*{Introduction}

This chapter covers Release~1, \textit{Foundations \& Core Marketplace},
which establishes the identity layer, the seller-onboarding loop and the
core selling capabilities the rest of the platform builds on. After a
release overview, use-case diagram, domain model and backlog, it
presents the release's two sprints: \textbf{Sprint~1 (Authentication \&
Seller Onboarding)} and \textbf{Sprint~2 (Store \& Product Management)}.

\section{Release 1 Overview}

Release~1 delivers the marketplace skeleton over two sprints. Sprint~1
builds authentication and the seller-onboarding loop, so that a visitor
can register, verify their email, sign in, and request the seller role
for an administrator to review. Sprint~2 builds store and product
management, so that an approved seller can open a store, customise its
storefront, populate a catalogue and run a store-wide sale, while
customers browse that catalogue by category. The architecture and the
technology stack that frame this realisation were established in
Section~\ref{chap:requirements} and are not repeated here; this release
instantiates them for the foundational features.

\subsection{Release 1 Domain Model}

Figure~\ref{fig:class-release1} shows the slice of the domain model
exercised by Release~1: the \texttt{User} with its role, the
\texttt{SellerApplication} onboarding record, the \texttt{Store} a seller
owns, and the product catalogue (\texttt{Product}, \texttt{ProductImage}
and \texttt{ProductTag}) organised by \texttt{Category} and
\texttt{Subcategory}.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/class-release1.png}
  \caption{Release~1 domain model (subset of the platform class diagram).}
  \label{fig:class-release1}
\end{figure}

\section{Release~1 Backlog}

The release backlog draws sixteen user stories from the product backlog
of Section~\ref{chap:requirements} and allocates them to the two
sprints, as summarised in Table~\ref{tab:backlog-r1}. Each sprint carries
an estimated duration, in line with the overall five-to-six-month
schedule.

\begin{table}[H]
  \centering
  \caption{Release~1 backlog, user stories grouped by sprint and feature.}
  \label{tab:backlog-r1}
  \footnotesize
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\textwidth}{|l|l|X|>{\centering\arraybackslash}p{1.9cm}|}
    \hline
    \rowcolor{tableHeader}
    \textcolor{white}{\textbf{Sprint}} & \textcolor{white}{\textbf{Feature}} &
    \textcolor{white}{\textbf{User stories}} & \textcolor{white}{\textbf{Est. time}} \\
    \hline
    \multirow{2}{*}{Sprint 1} & Authentication & US01 account creation, US02 email verification, US03 sign-in, US04 sign-out, US37 password reset & \multirow{2}{*}{\textasciitilde1 month} \\ \cline{2-3}
                              & Administration & US18 seller-application review & \\ \hline
    \multirow{2}{*}{Sprint 2} & Store Management & US05 store creation, US06 readable store slug, US07 storefront WYSIWYG editor, US08 style presets, US09 multiple stores (premium), US34 store-wide sales, US42 per-product sales & \multirow{2}{*}{\textasciitilde1.5 months} \\ \cline{2-3}
                              & Product Management & US10 product CRUD, US11 product images, US12 product tags, US13 category browsing & \\ \hline
    \multicolumn{3}{|r|}{\textbf{Release~1 total}} & \textbf{\textasciitilde2.5 months} \\ \hline
  \end{tabularx}
\end{table}

\section{Sprint 1, Authentication \& Seller Onboarding}

\subsection{Sprint 1 Objective}

Sprint~1 delivers the identity layer of the marketplace and the loop
that turns a customer into a seller. By the end of the sprint a visitor
can create an account secured by email verification, sign in and out
through database-backed sessions, recover access through an email-based
password reset, and submit a seller application; an administrator can
review that application and, on approval, promote the account to the
seller role. Authentication is provided by the
\textbf{Better-Auth} library, integrated into the NestJS backend through
a thin adapter; it ships email/password credentials, email verification,
database-persisted sessions and cookie transport, so that no
cryptography is hand-written. Every protected route is gated by an
authentication guard and the \texttt{@Roles()} decorator, with
object-level ownership enforced in the service layer.

\subsection{Sprint 1 Use-Case Diagram}

Figure~\ref{fig:uc-sprint1} details the sprint's use cases. Registration
\emph{includes} email verification; submitting a seller application
\emph{triggers} an administrative review, which may \emph{extend} into an
approval, a rejection or a request for more information.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/uc-sprint1.png}
  \caption{Sprint~1 use-case diagram (authentication and seller onboarding).}
  \label{fig:uc-sprint1}
\end{figure}

\subsection{Analysis and Design}

The registration and email-verification control flow is summarised in
Table~\ref{tab:act-registration-desc} and then modelled as an activity
diagram.

\subsubsection{Activity Diagram}

\begin{table}[H]
  \centering
  \caption{Textual description of the registration and email-verification activity.}
  \label{tab:act-registration-desc}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3.2cm}|X|}
    \hline
    Title & Register and verify email \\ \hline
    Objective & Create a marketplace account and confirm ownership of the email address before protected actions are unlocked. \\ \hline
    Primary actor & Visitor (unauthenticated). \\ \hline
    Secondary actor & Email Service (verification email). \\ \hline
    Pre-condition & No account exists for the supplied email address. \\ \hline
    Nominal scenario & 1.~The visitor opens the sign-up page and enters a name, email and password. \newline 2.~Better-Auth creates the user with the \textsc{customer} role and \texttt{emailVerified = false}. \newline 3.~A one-time verification token is generated and emailed. \newline 4.~The visitor opens the link and the token is validated. \newline 5.~\texttt{emailVerified} becomes \texttt{true}, unlocking store creation and order placement. \\ \hline
    Alternative scenario & A1.~Email already registered: the visitor is invited to sign in instead. \newline E1.~Invalid or expired token: verification is refused and a new link can be requested. \\ \hline
    Post-condition & A verified \texttt{User} with the \textsc{customer} role and an authenticated session. \\ \hline
  \end{tabularx}
\end{table}

Figure~\ref{fig:act-registration} models the registration and
email-verification control flow. Two decision points govern the path: a
uniqueness check on the email at sign-up, and a validity check on the
one-time token at verification. Only once the token is accepted and
\texttt{emailVerified} is set to \texttt{true} does the account unlock
the protected actions of store creation and order placement.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.82\textheight,keepaspectratio]{img/act-registration.png}
  \caption{Activity diagram, registration and email verification.}
  \label{fig:act-registration}
\end{figure}

\subsection{Realisation}

The sprint delivers the authentication and onboarding interfaces shown in
the following figures. Each frame is a placeholder for the corresponding
screenshot of the running application.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Registration page, name, email and password form}
  \caption{Registration page.}
  \label{fig:real-register}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Email-verification screen, ``check your inbox'' and verified confirmation}
  \caption{Email-verification screen.}
  \label{fig:real-verify}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Login page, credentials form and session redirect}
  \caption{Login page.}
  \label{fig:real-login}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Password-reset flow, ``forgot password'' request page and the new-password page reached from the emailed link}
  \caption{Password-reset pages.}
  \label{fig:real-reset}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[7cm]{Seller-application form, identity, business details and document upload}
  \caption{Seller-application form.}
  \label{fig:real-apply}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[7cm]{Administrator review panel, pending applications with approve / reject / request-more-info actions}
  \caption{Administrator seller-application review panel.}
  \label{fig:real-review}
\end{figure}

\section{Sprint 2, Store \& Product Management}

\subsection{Sprint 2 Objective}

Sprint~2 turns an approved seller into an active merchant. By the end of
the sprint a seller can create a store reachable at a human-readable
slug, customise its storefront across seven sections through a WYSIWYG
editor, populate a product catalogue with images and tags, and run
sales either store-wide or on individual products; customers can browse
that catalogue by category. The
storefront editor is built with \textbf{Craft.js}~\cite{craftjs}, where clicking any
section on the live canvas opens its settings panel, with undo/redo and
preset starting points. Several capabilities exposed here are
\textbf{premium-tiered}: the full customisation set, the operation of
multiple stores (US09) and the AI-assisted storefront redesign (US35) are
built on the storefront foundation in this sprint, but their premium
\emph{entitlement} is delivered by the premium tier in Release~2
(Chapter~\ref{chap:release2}); until then free accounts use the curated
subset and a single store.

\subsection{Sprint 2 Use-Case Diagram}

Figure~\ref{fig:uc-sprint2} details the sprint's use cases. Storefront
customisation \emph{includes} applying a preset and uploading a banner,
and \emph{extends} into the AI redesign; product management
\emph{includes} image upload and tagging.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth,keepaspectratio]{img/uc-sprint2.png}
  \caption{Sprint~2 use-case diagram (store and product management).}
  \label{fig:uc-sprint2}
\end{figure}

\subsection{Analysis and Design}

The store-creation and AI-redesign control flows are summarised in
Tables~\ref{tab:act-create-store-desc} and~\ref{tab:act-ai-redesign-desc}
and then each modelled as an activity diagram.

\subsubsection{Activity Diagrams}

\begin{table}[H]
  \centering
  \caption{Textual description of the store-creation activity.}
  \label{tab:act-create-store-desc}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3.2cm}|X|}
    \hline
    Title & Create store \\ \hline
    Objective & Open a store with a name, description and logo, reachable at a readable URL. \\ \hline
    Primary actor & Seller. \\ \hline
    Secondary actor & AI verification service. \\ \hline
    Pre-condition & The user holds the seller role; a free-tier seller owns no store yet. \\ \hline
    Nominal scenario & 1.~The seller opens the create-store form and enters a name, description and logo. \newline 2.~The system derives a slug from the name and enforces uniqueness, appending a numeric suffix on collision. \newline 3.~The store is persisted as \textsc{pending} and a \texttt{StoreStatusLog} entry is written. \newline 4.~The store is queued for AI verification. \newline 5.~The storefront editor opens on the default preset. \\ \hline
    Alternative scenario & A1.~A free-tier seller already owns a store: the seller is prompted to upgrade to premium. \newline E1.~Unsupported logo type or size: validation rejects the upload. \\ \hline
    Post-condition & A \textsc{pending} \texttt{Store} with a unique slug exists, logged and queued for verification. \\ \hline
  \end{tabularx}
\end{table}

Figure~\ref{fig:act-create-store} models store creation, including slug
derivation with collision handling and the transition to a
\textsc{pending} store queued for verification.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.82\textheight,keepaspectratio]{img/act-create-store.png}
  \caption{Activity diagram, store creation.}
  \label{fig:act-create-store}
\end{figure}

\begin{table}[H]
  \centering
  \caption{Textual description of the AI storefront-redesign activity.}
  \label{tab:act-ai-redesign-desc}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{|>{\bfseries}p{3.2cm}|X|}
    \hline
    Title & AI storefront redesign \\ \hline
    Objective & Regenerate the full storefront from a natural-language prompt. \\ \hline
    Primary actor & Seller (premium). \\ \hline
    Secondary actor & AI Service (n8n + Groq model). \\ \hline
    Pre-condition & The seller owns the store and is in the editor. \\ \hline
    Nominal scenario & 1.~The seller clicks ``AI Redesign'' and enters a prompt. \newline 2.~The backend checks ownership and forwards the prompt with the store name and description to the n8n workflow. \newline 3.~n8n builds the system prompt, calls the Groq model, then parses and sanitises the returned JSON. \newline 4.~The design is persisted and applied to the live canvas. \newline 5.~The seller reviews, refines or saves. \\ \hline
    Alternative scenario & E1.~The model returns invalid JSON: the sanitiser rejects it, the current design is kept and an error is shown. \\ \hline
    Post-condition & A complete, sanitised \texttt{StoreCustomization} covering all seven sections is applied to the canvas for review. \\ \hline
  \end{tabularx}
\end{table}

Figure~\ref{fig:act-ai-redesign} models the AI-redesign flow, from the
prompt to the sanitised design applied on the live canvas, with the
seller's review-and-save decision.

\begin{figure}[H]
  \centering
  \includegraphics[height=0.82\textheight,keepaspectratio]{img/act-ai-redesign.png}
  \caption{Activity diagram, AI storefront redesign.}
  \label{fig:act-ai-redesign}
\end{figure}

\subsection{Realisation}

The sprint delivers the store and product interfaces shown below. Each
frame is a placeholder for the corresponding screenshot of the running
application.

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Seller store dashboard, metrics, store status and quick actions}
  \caption{Seller store dashboard.}
  \label{fig:real-storedash}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[7cm]{Storefront WYSIWYG editor, Craft.js canvas with the seven-section settings panel}
  \caption{Storefront customisation editor.}
  \label{fig:real-editor}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{AI redesign dialog, natural-language prompt input}
  \caption{AI storefront-redesign dialog.}
  \label{fig:real-airedesign}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[6cm]{Product management, catalogue list with create/edit form, image upload and tags}
  \caption{Product management interface.}
  \label{fig:real-product}
\end{figure}

\begin{figure}[H]
  \centering
  \imagePlaceholder[5cm]{Sale panel: store-wide discount (percentage, label, expiry) and the per-product sale list}
  \caption{Sale configuration: store-wide and per-product.}
  \label{fig:real-sale}
\end{figure}

\section*{Conclusion}

Release~1 laid the foundations of the marketplace. Sprint~1 delivered the
identity layer, registration with email verification, sessions, password
reset and role-based access, plus the seller-onboarding loop. Sprint~2
turned approved sellers into active merchants, store creation, the
storefront editor, AI-assisted redesign, product management and
store-wide and per-product sales, and opened catalogue browsing to
customers. The
marketplace can now be populated, but not yet transact; Release~2 adds
that.

